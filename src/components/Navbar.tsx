import React, { ReactElement } from 'react'
import { Link } from 'office-ui-fabric-react';

type NavBarProps = {
  
}

const Navbar: React.FC = (props: NavBarProps) : ReactElement => {

  return (
    <div>
      
    </div>
  )
}

export default Navbar;